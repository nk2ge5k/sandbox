#ifndef COMMAND_TEST_CSV
#define COMMAND_TEST_CSV

#include "types.h"

// commandTestCsv is the command for testing CSV parsing, that I wrote
// becase I was too lazy to write an actual test.
i32 commandTestCsv(i32 argc, char **argv);

#endif
